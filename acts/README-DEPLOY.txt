ACTS NEWSLETTER 01 — standalone deployment

Source root:
  /acts

Build command:
  npm run build

Build output:
  /acts/dist

Important:
- This build is independent from the root A01 build.
- Do not deploy the repository root when publishing ACTS NEWSLETTER 01.
- Use /acts as the project root (working directory) and /acts/dist as the static output.
- Keep the existing hand-wizard-a01 site unchanged.
- Public-facing pages must not display the internal program name "손안의 마법사".

Current newsletter version: v1.3.0
